<% if can? :read, Facility.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :facilities]),
    label: t('facilities.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('facilities.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
