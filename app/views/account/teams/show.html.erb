<%= render 'account/shared/page' do |p| %>
  <% p.content_for :title, t('.header', teams_possessive: possessive_string(@team.name)) %>

  <% p.content_for :body do %>
    <%= render 'account/facilities/index', facilities: @team.facilities, hide_back: true %>
    <%= render 'account/training_programs/index', training_programs: @team.training_programs, hide_back: true %>
    <%= render 'account/locations/index', locations: @team.locations, hide_back: true %>
    <%= render 'account/pricing_models/index', pricing_models: @team.pricing_models, hide_back: true %>
    <%# 🚅 super scaffolding will insert new children above this line. %>

  <% end %>
<% end %>
