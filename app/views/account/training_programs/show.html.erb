<%# 🚅 Super scaffolding will insert new scaffolding above. %>

<div class="container mx-auto my-8">
  <div class="mb-6">
    <h1 class="text-3xl font-bold"><%= @training_program.name %></h1>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <% if @training_program.description.present? %>
      <div class="prose max-w-none mb-6">
        <%= @training_program.description %>
      </div>
    <% end %>

    <% if @training_program.training_contents.any? %>
      <div class="mt-8">
        <h2 class="text-xl font-semibold mb-4">Training Content</h2>
        <div class="space-y-4">
          <% @training_program.training_contents.each do |content| %>
            <div class="border rounded p-4">
              <%= content.title %> - <%= content.body %>
              <%# Add more content details as needed %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
